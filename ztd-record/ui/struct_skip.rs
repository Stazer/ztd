use ztd_record::Record;

////////////////////////////////////////////////////////////////////////////////////////////////////

#[derive(Record)]
struct Struct {
    #[Record(skip)]
    field: String,
}

////////////////////////////////////////////////////////////////////////////////////////////////////

fn main() {
    (Struct {
        field: String::from("foo"),
    }).into_record().field;
}
